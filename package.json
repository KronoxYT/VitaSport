{
  "name": "vitasport",
  "version": "1.0.0",
  "description": "Sistema de gestión de inventario para VitaSport.",
  "main": "src/main/main.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "tauri:build:win64": "tauri build --target x86_64-pc-windows-msvc",
    "tauri:build:win32": "tauri build --target i686-pc-windows-msvc",
    "tauri:build:linux": "tauri build --target x86_64-unknown-linux-gnu",
    "build:renderer": "cd src/renderer/react-app && npm ci && npm run build",
    "start-api": "node src/server.js",
    "start-renderer": "cd src/renderer/react-app && npm ci && npm run dev",
    "prepare-tauri": "npm run build:renderer",
    "test": "jest",
    "db:migrate": "node src/database/migrate.js"
  },
  "keywords": [],
  "author": "KronoxYT",
  "license": "ISC",
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "knex": "^3.1.0",
    "pdfmake": "^0.2.20",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "@tauri-apps/cli": "^2.0.0",
    "jest": "^29.7.0",
    "supertest": "^7.1.4"
  }
}
